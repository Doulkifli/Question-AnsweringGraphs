:auto Using Periodic Commit LOAD CSV with headers FROM "file:///FavoriteVotes.csv" as row match (n:User {UserId:toInteger(row.owneruserid)}) match (b:User {UserId:toInteger(row.userid)}) create (b)-[:FavoriteVotes{NbrOfVotes:row.NbrOfVotes}]-> (n)
