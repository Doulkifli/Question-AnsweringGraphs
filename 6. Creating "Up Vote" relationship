:auto Using Periodic Commit LOAD CSV with headers FROM "file:///UpVotes.csv" as row match (n:User {UserId:toInteger(row.OwnerUserId)}) match (b:User{UserId:toInteger(row.UserId)}) create (b)-[:UpVote]-> (n)
